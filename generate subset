def generate(arr):
    n=len(arr)
    totalSubsets=(1<<n)
    ans=[]
    for val in range(totalSubsets):
        temp=[]
        for i in range(n):
            if(val&(1<<i)):
                temp.append(arr[i])
        ans.append(temp)
        return ans
arr=[1,2,3]
ans=generate(arr)
print(ans)
